<div class="movie">
  <h1 class="movie-title">{{ movie.attributes.title }}</h1>
  <div class="intro">
    {{{ markdown movie.attributes.intro }}}
  </div>
  {{#if movie.attributes.image }}
  <img class="movie-img" src="{{ movie.attributes.image.url }}">
  {{/if }}
</div>